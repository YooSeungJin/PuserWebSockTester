{
  "name": "pusher_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify src/app.js -o disc/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^17.0.0"
  },
  "devDependencies": {
    "laravel-echo": "^1.10.0",
    "pusher-js": "^7.0.3"
  }
}
